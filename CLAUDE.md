# CLAUDE.md

## Stack
Next.js (App Router), TypeScript, plain CSS, Supabase (database and auth), deployed on Vercel.

## Commands
No custom build or test commands are set up yet. Use standard Next.js commands (npm run dev, npm run build) unless a future update to this file says otherwise.

## Never
- Add a dependency without asking first.
- Edit .env or any environment variable.
- Change auth configuration without saying what is changing and why.
- Create new top-level folders.

## Conventions
Plain CSS only, no CSS framework. Keep components small and readable — Jeff has no coding background and needs to be able to follow what each file does.

## Current focus
See roadmap.md. Work only on the slice marked ACTIVE.
